package ${packageInfo.mapperPackage};

import com.winter.business.mapper.BaseMapper;
import ${packageInfo.voPackage}.${tableInfo.voClassName};


/**   
 * @Title: ${noteInfo.title}
 * @Description: ${noteInfo.description}
 * @author ${noteInfo.author}
 * @date ${noteInfo.date}
 * @version 1.0
 */


public interface ${tableInfo.mapperClassName} extends BaseMapper<${tableInfo.voClassName}> {

}
